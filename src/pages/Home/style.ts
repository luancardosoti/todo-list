import styled from 'styled-components'

export const HomeContainer = styled.div`
  padding-bottom: 2rem;

  main {
    max-width: 46rem;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 4rem;
    padding: 0 1rem;
  }
`
